// Formula igual a aula. 1 
let x = 76, y = 1.79;           
let resultado = x / y ** 2;

console.log(`Peso (${x} / ${y} * ${y} ) = ${resultado.toFixed(1)}`);

// formula igual ao README.
let peso = 76;
let altura = 1.79 ^ 2;
let imc = peso / altura;

console.log(imc.toFixed(2));
