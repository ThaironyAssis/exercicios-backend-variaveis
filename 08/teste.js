// Variáveis
let raio = 1; // raio do círculo

// Cálculo do perímetro
let perimetro = 2 * Math.PI * raio;

// Cálculo da área
let area = Math.PI * Math.pow(raio, 2);

console.log("Perímetro do círculo: " + perimetro);
console.log("Área do círculo: " + area);